function Node(value) {
    this.value = value;
    this.leftNode = null;
    this.rightNode = null;
}

function treeSort(data, sortParam) {

    // Build binary tree

    // set root node
    const rootNode = new Node(0);
    console.log(rootNode);
    // set smaller left
    // set larger right
    // repeat


    // Read binary tree

}

treeSort();